import styled from 'styled-components/macro';

const StyledToolbar = styled.div`
    flex-shrink: 0;
    height: 1.5rem;

    border-bottom: #434759 2px solid;

    background-color: #272a35;
`;

export default StyledToolbar;
